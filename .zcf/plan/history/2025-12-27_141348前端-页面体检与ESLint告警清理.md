# 前端-页面体检与ESLint告警清理

生成时间：2025-12-27 14:11:46

## 背景
- 已修复“系统日志（调试面板）入口与底部发言区重叠”的布局问题。
- 需要继续完成前端页面体检：清理 ESLint warnings，并做更完整的静态检查。

## 目标（可验证）
- `frontend/`：`npm run lint` 输出 0 warning / 0 error（至少本次相关告警清零）
- `frontend/`：`npm run build` 通过
- `frontend/`：TypeScript 类型检查通过（`tsc --noEmit`）

## 计划
1. 复现并定位 ESLint warnings 来源（当前为 `react-refresh/only-export-components`）
2. 采用最小改动策略处理：对 `src/components/ui/**` 这类“组件库文件”禁用该规则，避免为 fast-refresh 限制做大规模拆文件
3. 运行 `lint` / `build` / `tsc` 做质量门禁
4. 归档本计划到 `.zcf/plan/history/`

